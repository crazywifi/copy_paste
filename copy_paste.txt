nmap -p- --open -n -Pn -T4 -sV <IP> -oG - | awk '
/open/ {
    split($2, ip, " "); 
    split($1, port, "/"); 
    if ($0 ~ /https/) 
        print "https://" ip[1] ":" port[1]; 
    else 
        print "http://" ip[1] ":" port[1];
}' | tee urls.txt
