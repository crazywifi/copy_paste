const originalHtml = $.fn.html;
$.fn.html = function(arg) {
  if (typeof arg === 'string' && (arg.indexOf('<') !== -1 || arg.indexOf('>') !== -1)) {
    console.log("[html() called with]:", arg);
    console.trace();
  }
  return originalHtml.apply(this, arguments);
};
